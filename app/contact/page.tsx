import React from "react";
import { FinalCTA } from "@/components/FinalCTA";

export const metadata = {
  title: "Contact — Uptrends Solutions",
  description:
    "Start a conversation: request a free consultation for branding, media, or digital platforms.",
};

export default function ContactPage() {
  return (
    <main>
      <FinalCTA />
    </main>
  );
}
